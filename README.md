# Agro-Waste Recommender (vanilla HTML/CSS/JS)

A 4-step static site (Context → Typology → Approach → Result) that generates a recommendation from a decision table.  
No framework, no build; GitHub Pages-ready.
